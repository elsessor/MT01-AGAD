- POST `/students` → Add student (validated)
- GET `/students` → List all students
- GET `/students/:id` → Get student by ID
- PUT `/students/:id` → Update student
- DELETE `/students/:id` → Remove student

Run:
```bash
cd backend
npm install
npm run dev
```

Health check: `GET /health`
